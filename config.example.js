// Mapbox Configuration Example
// This is a template file that CAN be safely uploaded to GitHub
// Copy this file to config.js and add your actual token

const CONFIG = {
    MAPBOX_TOKEN: 'YOUR_MAPBOX_ACCESS_TOKEN'
};
